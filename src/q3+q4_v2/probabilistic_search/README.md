# 概率推断＋试探清除＋短程成本搜索

入口：`python run.py --mode q4 --seed 101 --n-sources 10 --scenario mixed`。

默认 Q4 使用 sparse25 严格证书、前 3 个证书点主动复测；Q3 中心点若发现至少 6 个 ACTIVE 频道，兜底无收缩阈值提高到 18，否则为 9，Q4 为 8；试探评分只改变动作顺序。

策略维护可观察状态上的存在概率、角度不确定度和试探清除的成本收益代理。概率只负责排序；任务结束仍由严格的源清除或有效无源证书决定，未完成不会被主动退出伪装成成功。
